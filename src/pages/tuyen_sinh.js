import Footer from "../components/footer";
import Header from "../components/header";

const tuyeSinh = {
    render() {
        return /* html */`
        ${Header.render()}
        <p>Tuyển sinh</p>
        ${Footer.render()}
        
        `;
    },
};

export default tuyeSinh;